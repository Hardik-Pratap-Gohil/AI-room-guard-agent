# AI Room Guard System - Dependencies
# Python 3.13 compatible versions

# ============================================
# IMPORTANT: Install dlib FIRST (Windows)
# ============================================
# dlib needs a custom wheel for Windows Python 3.13
# The below line ensures it installs from GitHub directly
https://github.com/omwaman1/dlib/releases/download/dlib/dlib-19.24.99-cp313-cp313-win_amd64.whl

# Computer Vision (requires dlib above)
face-recognition==1.3.0
opencv-python==4.12.0.88

# Speech Recognition
SpeechRecognition==3.14.3

# Text-to-Speech
gTTS==2.5.4
pygame==2.6.1

# LLM Integration
google-generativeai==0.8.5

# Core Libraries
numpy==2.1.3

# ============================================
# INSTALLATION INSTRUCTIONS
# ============================================
#
# Windows users (Python 3.13):
#   pip install -r requirements.txt
#   (dlib will install automatically from GitHub)
#
# Linux/macOS users:
#   1. Install system dependencies first:
#      Ubuntu/Debian:
#        sudo apt-get update
#        sudo apt-get install python3-dev cmake
#        sudo apt-get install portaudio19-dev
#      
#      macOS:
#        brew install cmake
#        brew install portaudio
#
#   2. Then install:
#      pip install dlib
#      pip install -r requirements.txt
#
# ============================================